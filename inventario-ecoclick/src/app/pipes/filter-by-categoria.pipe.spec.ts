import { FilterByCategoriaPipe } from './filter-by-categoria.pipe';

describe('FilterByCategoriaPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterByCategoriaPipe();
    expect(pipe).toBeTruthy();
  });
});
