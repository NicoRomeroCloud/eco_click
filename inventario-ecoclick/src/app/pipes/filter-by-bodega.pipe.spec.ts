import { FilterByBodegaPipe } from './filter-by-bodega.pipe';

describe('FilterByBodegaPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterByBodegaPipe();
    expect(pipe).toBeTruthy();
  });
});
